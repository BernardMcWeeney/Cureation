---
interface Props {
  variant?: 'default' | 'accent';
  href?: string;
  class?: string;
}
const { variant = 'default', href, class: klass = '' } = Astro.props;
const classes = ['tag', variant === 'accent' ? 'tag--accent' : '', klass].filter(Boolean).join(' ');
---
{href ? (
  <a href={href} class={classes}><slot /></a>
) : (
  <span class={classes}><slot /></span>
)}

<style>
  .tag--accent { color: var(--accent-3); }
  :global(:root[data-theme='papers']) .tag--accent { color: var(--accent); }
  a.tag { border-bottom-width: 1px; }
  a.tag:hover { color: var(--accent-3); border-color: var(--accent); }
  :global(:root[data-theme='papers']) a.tag:hover { color: var(--accent); }
</style>
