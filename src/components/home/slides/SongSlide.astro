---
import type { SongWithAlbum } from '../../../types/directus';
import { generateSlug } from '../../../lib/directus';

interface Props {
  song: SongWithAlbum;
}

const { song } = Astro.props;
const songSlug = generateSlug(song.title);
---

<a href={`/lyrics/${songSlug}`} class="flex flex-col items-start justify-end h-full w-full p-8 md:p-12 group">
  <div class="max-w-xl">
    <span class="text-text-muted text-xs font-medium uppercase tracking-[0.15em] mb-4 block">Song of the Day</span>

    <h2 class="font-heading text-4xl md:text-5xl lg:text-6xl text-white mb-3 text-shadow-lg leading-tight">
      {song.title}
    </h2>

    <div class="flex flex-wrap gap-2 mb-6">
      <span class="px-3 py-1 bg-black/40 backdrop-blur-sm border border-border-default text-sm text-text-secondary text-shadow">
        {song.albumTitle}
      </span>
      {song.year && (
        <span class="px-3 py-1 bg-black/40 backdrop-blur-sm border border-border-default text-sm text-text-secondary text-shadow">
          {song.year}
        </span>
      )}
    </div>

    <div class="flex items-center gap-2 text-primary text-sm opacity-80 group-hover:opacity-100 transition-all duration-300">
      <span>Read lyrics</span>
      <svg xmlns="http://www.w3.org/2000/svg" class="size-4 group-hover:translate-x-1 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path>
      </svg>
    </div>
  </div>
</a>
