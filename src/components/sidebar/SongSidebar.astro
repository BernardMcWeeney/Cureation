---
// Sidebar for song/lyrics pages - shows album tracklist with current highlighted
import { generateSlug } from '../../lib/directus';

interface Track {
  number: number;
  title: string;
  slug: string;
  duration: string;
}

interface Props {
  currentSong: {
    title: string;
    trackNumber: number;
  };
  album: {
    title: string;
    slug: string;
    coverImage: string;
    year: string;
  };
  albumTracks: Track[];
}

const { currentSong, album, albumTracks } = Astro.props;

const currentSlug = generateSlug(currentSong.title);
---

<aside class="sticky top-24 w-full space-y-6">
  <!-- Album Card -->
  <a
    href={`/discography/${album.slug}`}
    class="block glass-surface rounded-xl overflow-hidden hover:border-white/15 transition-all duration-300 group"
  >
    <div class="relative aspect-square">
      <img
        src={album.coverImage}
        alt={album.title}
        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
      />
      <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent" />
      <div class="absolute bottom-0 left-0 right-0 p-4">
        <p class="text-white font-medium group-hover:text-white transition-colors">{album.title}</p>
        <p class="text-white/85 text-sm">{album.year}</p>
      </div>
    </div>
  </a>

  <!-- Album Tracklist -->
  <div class="glass-surface rounded-xl overflow-hidden">
    <div class="p-4 divider-theme border-b">
      <h2 class="font-heading text-xl text-primary">Tracklist</h2>
    </div>
    <nav class="p-2 max-h-96 overflow-y-auto">
      <ul class="space-y-1">
        {albumTracks.map((track) => {
          const trackSlug = generateSlug(track.title);
          const isCurrent = trackSlug === currentSlug;
          return (
            <li>
              <a
                href={`/lyrics/${trackSlug}`}
                class={`flex items-center gap-3 px-3 py-2 rounded-lg text-sm transition-all duration-300 ${
                  isCurrent
                    ? 'bg-white/10 text-primary'
                    : 'text-white hover:bg-white/10 hover:text-white'
                }`}
                aria-current={isCurrent ? 'page' : undefined}
              >
                <span class={`w-5 text-right ${isCurrent ? 'text-primary' : 'text-white/80'}`}>
                  {track.number}
                </span>
                <span class="flex-1 truncate">{track.title}</span>
                <span class={`text-xs ${isCurrent ? 'text-primary/70' : 'text-white/80'}`}>
                  {track.duration}
                </span>
              </a>
            </li>
          );
        })}
      </ul>
    </nav>
  </div>

  <!-- Quick Actions -->
  <div class="glass-surface rounded-xl p-4">
    <div class="flex gap-2">
      <a
        href={`/discography/${album.slug}#tracks`}
        class="flex-1 text-center px-3 py-2 rounded-lg glass-elevated hover:bg-white/10 text-white text-sm transition-colors duration-300"
      >
        View Album
      </a>
      <a
        href="/lyrics"
        class="flex-1 text-center px-3 py-2 rounded-lg glass-inset hover:bg-white/10 text-white text-sm transition-colors duration-300"
      >
        All Lyrics
      </a>
    </div>
  </div>
</aside>
