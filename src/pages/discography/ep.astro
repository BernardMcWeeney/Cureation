---
import Layout from '../../layouts/Layout.astro';
import AlbumCard from '../../components/album/AlbumCard.astro';
import IssueSection from '../../components/ui/IssueSection.astro';
import Crumbs from '../../components/ui/Crumbs.astro';
import Chip from '../../components/ui/Chip.astro';
import { listAlbums } from '../../lib/directus';

const eps = await listAlbums({ type: 'ep' });
const tabs = [
  { href: '/discography',              label: 'Studio',      active: false },
  { href: '/discography/ep',           label: 'EPs',         active: true  },
  { href: '/discography/compilations', label: 'Compilations',active: false },
  { href: '/discography/singles',      label: 'Singles',     active: false },
];
---
<Layout title="EPs · Discography · Cureation">
  <div class="container" style="padding-top: clamp(24px, 3vw, 40px);">
    <Crumbs items={[{ label: 'Home', href: '/' }, { label: 'Discography', href: '/discography' }, { label: 'EPs' }]} />
    <div class="stack" style="--gap: 1rem; margin-top: 1rem;">
      <div class="mono-sm muted">SECTION · EXTENDED PLAYS</div>
      <h1 class="serif" style="font-size: clamp(48px, 7.5vw, 96px); line-height:.95; font-style: italic; margin: 0;">Extended plays</h1>
      <p class="deck" style="max-width: 62ch;">Short-form releases and interstitial records — the missives between albums.</p>
      <div class="cluster" style="gap: 6px; flex-wrap: wrap;">
        {tabs.map((t) => <Chip href={t.href} active={t.active}>{t.label}</Chip>)}
      </div>
    </div>
  </div>

  <IssueSection kicker={`${eps.length} records`} title="EPs">
    {eps.length > 0 ? (
      <table class="data">
        <thead><tr><th style="width:50px;">№</th><th style="width:80px;">Year</th><th>Record</th><th class="num" style="text-align:right;">Tracks</th></tr></thead>
        <tbody>
          {eps.map((a, i) => (
            <tr data-clickable onclick={`location.href='/discography/${a.slug}'`}>
              <td class="idx">{String(i + 1).padStart(2, '0')}</td>
              <td class="mono-sm muted">{a.release_date?.slice(0, 4) || '—'}</td>
              <td><span class="serif" style="font-size:22px; font-style:italic;">{a.title}</span></td>
              <td class="num">{a.track_count || '—'}</td>
            </tr>
          ))}
        </tbody>
      </table>
    ) : (
      <p class="deck">No EPs catalogued yet.</p>
    )}
  </IssueSection>

  {eps.length > 0 && (
    <IssueSection kicker="Cover plates" title="Sleeves">
      <div class="records-grid">
        {eps.map((a) => <AlbumCard album={a} size="md" />)}
      </div>
    </IssueSection>
  )}
</Layout>

<style>
  .records-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: clamp(20px, 2.4vw, 36px); }
</style>